import React from 'react'

/**
 *  Option for user to follow / unfollow
 *  Avatar / name / mention / brif bio
 *  Show post history to followers if profile is set to "followers only".
 *  Show post history to everyone if profile is set to "public"
 *
 */

function UserProfile() {
  return <div>User Profile</div>
}

export default UserProfile
